# Copy to .env for local development (do not commit .env)
REACT_APP_BASE_URL=http://localhost:3001/api

# Production (Railway) — set this value in Vercel → Environment Variables → Production
# REACT_APP_BASE_URL=https://recruitment-ms-be-production.up.railway.app/api
